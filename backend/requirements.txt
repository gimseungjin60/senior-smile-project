fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart>=0.0.9
# 서버측 비전: 얼굴 DNN 감지(main.py) + LiveKit 프레임 변환(livekit_publisher.py).
# headless = GUI 라이브러리(libGL) 불필요 → 클라우드 이미지 경량화.
# 인지게임/스트레칭 비전은 브라우저 MediaPipe Web으로 이전됨(서버 mediapipe 제거,
# deprecated /ws/vision 만 잔존하며 호출 시 graceful 에러 반환).
opencv-python-headless>=4.8.0
attrs>=23.0.0
# firebase-admin / google-cloud-firestore 가 protobuf>=4.25.8 요구. <5 로 상한 고정(호환 안전).
protobuf>=4.25.8,<5
httpx>=0.27.0
Pillow>=10.0.0
numpy>=1.24.0
# STT 는 브라우저 마이크 발화를 OpenAI Whisper API 로 전사. 로컬 마이크/스피커 의존 제거됨
# (PyAudio·pygame·SpeechRecognition 삭제 — 오디오 I/O 가 갤탭 브라우저로 이전).
openai>=1.0.0
firebase-admin>=6.0.0
APScheduler>=3.10.0
python-dotenv>=1.0.0
PyJWT>=2.8.0
bcrypt>=4.0.0
# LiveKit publisher (실시간 영상)
livekit>=1.1.0
livekit-api>=1.0.0
