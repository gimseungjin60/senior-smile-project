# Python 가상환경 / 캐시
backend/.venv/
backend/venv/
backend/.venv-*/
**/__pycache__/
*.pyc

# macOS
.DS_Store

# 에디터
.idea/
.vscode/

# Node (frontend/.gitignore에도 있지만 루트에서도 제외)
frontend/node_modules/
frontend/dist/

# 빌드 산출물 (frontend vite build → backend/dist 로 복사됨, 커밋 안 함)
backend/dist/

# 사진 프록시 캐시 (런타임 생성)
backend/photo_cache/

# 보안 키/환경변수
backend/.env
backend/serviceAccountKey.json
backend/device_id.json
backend/users_local.json
backend/voice_messages/
*.env

# 런타임 상태 / 임시 파일
backend/greeting_state.json
backend/sounds/temp_*

# 대용량 모델 (필요 시 LFS로 별도 관리)
backend/models/*.onnx

# Claude 로컬 작업 메모
.claude/plans/
.claude/settings.local.json
